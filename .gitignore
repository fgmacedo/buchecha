# Binaries
/bcc
/buchecha
*.exe
*.dll
*.so
*.dylib

# Test artifacts
*.test
*.out
coverage.txt
coverage.html

# Go workspace
go.work
go.work.sum

# Local env / secrets
.env
.env.local
.env.*.local

# Local bcc config (contains absolute paths and machine-specific env vars).
# Each contributor runs `bcc init` to create their own.
.bcc.toml

# Per-iteration raw stream-json audit logs written by executor adapters.
.bcc/

# Editor / IDE
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# Claude Code per-machine state. The committed .claude/launch.json
# sits alongside and is shared across contributors.
.claude/settings.local.json
.claude/projects/
.claude/worktrees/

# Build output
/dist/
/build/

# SPA toolchain (internal/webui/web/)
internal/webui/web/node_modules/
internal/webui/web/.vite/
internal/webui/web/*.tsbuildinfo

# Runtime / scratch
/tmp/
.bcc-cache/
.claude/scheduled_tasks.lock
.compare-worktrees/
testdata/diag-dag-output/
testdata/bcc-validation/