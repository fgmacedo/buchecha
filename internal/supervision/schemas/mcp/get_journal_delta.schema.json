{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "get_journal_delta",
  "description": "Reviewer reads the journal text appended during the audited iteration. The handler computes the delta via the active spec format adapter.",
  "type": "object",
  "additionalProperties": false,
  "required": ["agent_id"],
  "properties": {
    "agent_id": { "type": "string", "minLength": 1 }
  }
}
