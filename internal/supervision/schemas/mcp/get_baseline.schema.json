{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "get_baseline",
  "description": "Reviewer reads the phase baseline SHA (stable across all attempts) and the current HEAD SHA so it can run git diff/log/show via Bash.",
  "type": "object",
  "additionalProperties": false,
  "required": ["agent_id"],
  "properties": {
    "agent_id": { "type": "string", "minLength": 1 }
  }
}
